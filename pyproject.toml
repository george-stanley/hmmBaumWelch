[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "hmmBaumWelch"

[tool.poetry.dependencies]
python = "^3.9"
numpy = "<=1.26.0,>=1.18.0"
scipy = ">=1.6.0"
